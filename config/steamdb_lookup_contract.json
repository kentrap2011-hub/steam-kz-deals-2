{
  "contract": "STEAMDB-TRUE-MISS-LOOKUP-V1",
  "version": "1.0",
  "status": "implementation_only",
  "purpose": "Resolve only stage-15 true SteamDB history misses with one exact source attempt per provider/entity key and publish a compact lookup ledger for stage 16.",
  "authority": {
    "canonical_policy_path": "config/mailing_policy.json",
    "never_overrides_canonical_policy": true,
    "steamdb_is_history_only": true,
    "lookup_only_stage_15_true_misses": true,
    "no_retry_same_provider_entity_key_within_one_run": true
  },
  "source": {
    "provider": "SteamDB",
    "app_url": "https://steamdb.info/app/{steam_id}/",
    "sub_url": "https://steamdb.info/sub/{steam_id}/",
    "region_row": "Kazakhstani Tenge",
    "currency": "KZT"
  },
  "result_rules": {
    "http_200_with_kazakhstani_tenge_row_and_nonzero_lowest": "confirmed_min",
    "http_200_with_kazakhstani_tenge_row_showing_free_or_zero_lowest": "previously_free",
    "http_200_or_404_where_exact_entity_history_is reached but Kazakhstan exact history is absent": "unavailable_exact_history",
    "403_429_timeout_network_or_5xx": "blocked_or_failure; never persist as negative cache"
  },
  "diagnostic_stage_16": {
    "producer_workflow": ".github/workflows/build-steamdb-true-miss-lookups.yml",
    "output": "data/cache/steamdb_lookup.validation.json",
    "pass_requires": [
      "every stage-15 true miss has exactly one registry entry",
      "no non-miss key is queried",
      "no duplicate provider/entity key is attempted",
      "confirmed minima are exact KZT values from the Kazakhstan row",
      "blocked or tool failures are separate from unavailable exact history",
      "all true misses are resolved without blocked/failure for PASS"
    ]
  }
}
