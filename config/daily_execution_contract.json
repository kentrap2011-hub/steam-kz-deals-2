{
  "contract": "DAILY-VISUAL-EXECUTION-V2",
  "status": "canonical",
  "timezone": "Europe/Samara",
  "ownership_contract": "config/execution_ownership_contract.json",
  "execution_invariants": {
    "daily_required_scope_must_not_be_intentionally_spread_across_multiple_days": true,
    "per_day_item_quota_allowed": false,
    "batching_semantics": "Internal checkpointing only within the same required production cycle; a batch size is never a daily throughput limit unless a future canonical contract explicitly says so.",
    "partial_backlog_is_not_normal_success": true,
    "temporary_handoff_or_legacy_task_must_not_override_this_contract": true,
    "separate_recurring_stage_requires_explicit_canonical_contract": true,
    "github_owns_control_plane": true,
    "interactive_chat_is_production_executor": false,
    "user_correction_requires_architecture_recheck_before_implementation": true,
    "rule": "The nightly production is one daily production cycle. GitHub owns the control plane, scope, queue/manifests, retry/completeness logic, deterministic processing and downstream orchestration. Scheduled ChatGPT performs only the explicit external/semantic data-plane work assigned by canonical contracts. Interactive user chats are for development/diagnosis and must not become manual production backlog processors. Do not convert checkpoint sizes, legacy task limits, temporary handoff notes or user corrections into architectural changes without canonical contract reconciliation."
  },
  "commercial_refresh": {
    "maximum_frequency": "once_per_day",
    "github_collection_local_time": "00:10",
    "network_refreshes_during_day_for_visualization": false,
    "rule": "Treat the completed nightly GitHub snapshot as the commercial state for the whole day. Do not fetch fresher price, discount, review, history, Store or SteamDB data when the user opens or refreshes the visualization."
  },
  "night_input_freshness": {
    "source_timestamp_field": "source_mailing_updated_at_utc",
    "maximum_age_hours_at_preparation": 18,
    "fail_closed_if_missing_or_unparseable": true,
    "fail_closed_if_older_than_maximum": true,
    "stale_action": "do not replace the last successful daily visual payload",
    "rule": "The 01:00 preparation may consume only a complete pre-AI payload whose source_mailing_updated_at_utc is present, parseable and no more than 18 hours old at preparation time. This prevents silent reuse of an old commercial snapshot after a failed nightly GitHub chain."
  },
  "known_expiry_guard": {
    "requires_network": false,
    "apply_during_night_preparation_only": true,
    "rule": "Known sale_end_utc values may be applied while producing the daily snapshot. Opening or refreshing the visualization during the day must not perform a new semantic or commercial recalculation."
  },
  "night_preparation": {
    "local_time": "01:00",
    "control_plane_owner": "GitHub repository and GitHub Actions",
    "external_semantic_worker": "ChatGPT scheduled task",
    "responsibilities": [
      "GitHub prepares the exact current production scope and explicit ChatGPT work inputs",
      "ChatGPT consumes only the completed GitHub-prepared inputs assigned to it by canonical contracts",
      "verify source_mailing_updated_at_utc is present, parseable and no more than 18 hours old; otherwise fail closed without replacing the last successful daily visual payload",
      "finish any required price-blind taste verdicts and persist them through the repository-defined taste inbox/checkpoint interface",
      "obtain required external facts that GitHub cannot access directly, only for exact GitHub-prepared entity keys",
      "return structured runtime/semantic results through repository-defined artifact interfaces without redefining scope, queue, retry policy or completeness",
      "GitHub validates and persists returned facts, closes the current production contract and rebuilds downstream artifacts",
      "consume precomputed purchase context and deal scenarios",
      "produce all user-facing semantic fields including eligibility, offer-family choice, deal verdict, reasons, risks and strict priority_rank",
      "build and persist data/production/visual/current.json through the daily visual producer",
      "allow the read-only Pages deploy to publish that prepared payload",
      "on failure do not replace the last successful complete visual payload"
    ]
  },
  "visualization_consumption": {
    "mode": "pull_on_demand",
    "scheduled_delivery": false,
    "user_behavior": "The user opens the visualization at any convenient time during the day and sees the already prepared frozen daily snapshot.",
    "source_artifact": "data/production/visual/current.json",
    "allowed_work": [
      "read the prepared visual payload",
      "render prepared fields",
      "manage local UI state such as tabs, gallery, cursor, progress and explicit manual queue changes"
    ],
    "forbidden_work": [
      "taste re-evaluation",
      "Steam or web refresh",
      "SteamDB refresh",
      "review refresh",
      "price or discount refresh",
      "history refresh",
      "deal scenario recalculation",
      "risk calculation",
      "reason calculation",
      "priority or ranking calculation",
      "eligibility or offer-family recalculation"
    ],
    "rule": "Opening or refreshing the visualization is read-only with respect to daily semantics and external commercial data."
  },
  "morning_delivery": {
    "enabled": false,
    "deprecated": true,
    "local_time": null,
    "previous_local_time": "08:00",
    "owner": null,
    "rule": "The separate 08:00 mailing/delivery is abolished. Do not schedule, recreate or attempt a morning push delivery; the visualization replaces it."
  },
  "daily_ready_manifest": {
    "deprecated": true,
    "not_part_of_current_user_delivery": true,
    "schema_version": 1,
    "required_fields": [
      "schema_version",
      "status",
      "source_mailing_updated_at_utc",
      "intended_delivery_local_date",
      "prepared_at_utc",
      "ready_text_sha256"
    ],
    "schema_version_value": 1,
    "status_complete_value": "complete",
    "source_binding_rule": "legacy compatibility only",
    "delivery_date_rule": "legacy compatibility only",
    "prepared_at_rule": "legacy compatibility only",
    "text_integrity_rule": "legacy compatibility only",
    "validation_workflow": ".github/workflows/validate-daily-handoff.yml",
    "fail_closed": true,
    "rule": "This block is retained only so the old handoff validator and historical artifacts remain understandable. It must not be used to recreate an 08:00 delivery."
  },
  "artifacts": {
    "canonical_visual_payload": "data/production/visual/current.json",
    "legacy_ready_text": "data/production/daily_ready/latest.md",
    "legacy_ready_manifest": "data/production/daily_ready/latest.json",
    "legacy_failure_status": "data/production/daily_ready/preparation_status.json"
  }
}
