# CURRENT_TASK

status: in_progress
started_at: 2026-08-30 Europe/Berlin
current_task: Довести новую финальную сортировку до фактического `data/production/visual/current.json`, устранив блокирующий history/SteamDB gate без потери уже полученных данных.
last_progress: Пользователь явно решил не тратить сейчас значимое время на исправление пяти SteamDB retry и утвердил архитектурное изменение: система должна сохранять уже успешно подтверждённые SteamDB-данные даже при наличии незакрытых ключей. Completeness и persistence разделяются: `529/534` остаётся incomplete stage, пять ключей продолжают жить в GitHub-owned retry state, но 529 validated facts должны проходить prepared artifact → validation → checkpoint в `steamdb_history.json`. Architecture preflight пройден: ownership остаётся у GitHub; scope, retry, completeness, validation, cache merge и downstream orchestration не переносятся в ChatGPT/interactive chat; batch не становится daily quota. Также выявлен отдельный hard gate в `.github/workflows/build-daily-visual-payload.yml`: visual replacement сейчас требует `history_snapshot.cache_status_counts.missing == 0`, хотя `build_pre_ai_history_snapshot.py` уже корректно классифицирует missing как `history_quality=unverified` и purpose=`pre_ai_nonblocking_price_history_classification`. Поэтому после partial checkpoint оставшиеся пять всё равно блокировали бы visual; этот gate нужно согласованно сделать nonblocking для отсутствующей второстепенной history, сохранив complete snapshot coverage и маркировку `unverified`.
next_step: Обновить canonical SteamDB lookup/checkpoint contracts под verified partial persistence, ingest всегда должен выпускать prepared resolutions из всех resolved facts, validator должен валидировать partial ledger без требования zero unresolved, checkpoint должен merge только validated registry и не требовать full stage coverage, ingest workflow должен запускать validation при наличии validated successes независимо от complete status. Затем снять `missing == 0` gate из visual workflow, оставив требования `history_snapshot.status == complete` и `complete_coverage == true`. После коммитов проверить CI/runtime artifacts и фактический rebuild `history_snapshot` → `current.json`; пять retry не обрабатывать вручную. Windows compatibility evidence-source остаётся отдельным последующим участком.
