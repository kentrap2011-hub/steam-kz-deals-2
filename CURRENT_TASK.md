# CURRENT_TASK

status: in_progress
started_at: 2026-08-30 Europe/Berlin
current_task: Проверить и исправить текущую логику итоговой сортировки/приоритета игр в витрине Steam KZ.
last_progress: Архитектурная ошибка двух независимых production-сортировок исправлена в production path. Добавлены `config/final_ranking_policy.json` (`FINAL-PRIORITY-RANKING-V1`), единый helper `scripts/priority_ranking.py`, единый production entrypoint `scripts/build_final_visual_payload.py`; `.github/workflows/build-daily-visual-payload.yml` больше не запускает старую последовательность `builder final sort -> refiner final sort`, а сначала валидирует ranking contract и затем вызывает один producer, который выполняет enrichment/refinement и только после этого один раз назначает `priority_rank`. Согласованный automatic order: `sale expiry today/tomorrow -> priority_bucket -> serious confirmed personal/Windows risk -> wishlist -> discount percent -> history quality -> current price -> achievement quality -> duration -> title`. Direct user evidence не является отдельным final factor, потому что уже корректирует fit/bucket. Средние/слабые эвристические риски не дают раннего штрафа. Голая Steam XP/7/8 label нейтральна. UI `В конец очереди` уже имел правильный локальный `manual_end_at` override и теперь защищён regression test: он абсолютнее automatic rank, включая expiring-today. Добавлены `scripts/validate_priority_ranking.py`, `PROJECT_DECISIONS.md` с rationale/rejected alternatives и обновлён быстрый маршрут `PROJECT_ROUTES.md`. GitHub Actions run `33297788592` на commit `b878289e...` прошёл: unified producer импортируется, exact contract/order и все regression cases проходят (`PRIORITY_RANKING_VALIDATION=PASS`). Полный новый `current.json` пока не пересобран, потому что отдельный существующий history gate сообщает `HISTORY_READY=false missing=534 complete_coverage=True primary_count=659` и корректно пропускает producer. Отдельно выявлен честный незакрытый участок Windows: ranking уже умеет понижать только подтверждённый `modern_windows_friction`, но существующий `build_visual_feed_v2.py` получает лишь старую Steam system-requirements строку; канонического автоматического источника фактической современной Windows-совместимости пока нет.
next_step: Не возвращаться к исследованию самой сортировки — начать с `PROJECT_ROUTES.md -> Финальная сортировка / priority rank витрины` и `PROJECT_DECISIONS.md`. Спроектировать и реализовать канонический источник подтверждённых modern-Windows compatibility facts, не полагающийся на одну Steam XP/7/8 label и не нарушающий ownership contract. После того как независимый history/SteamDB gate снова станет ready, выполнить обычный canonical visual build и проверить, что новый `data/production/visual/current.json` содержит `priority_ranking_contract=FINAL-PRIORITY-RANKING-V1`, новый `priority_factors` и ожидаемый ranking review; только после этого считать задачу E2E закрытой.
