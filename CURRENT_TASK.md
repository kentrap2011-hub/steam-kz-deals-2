# CURRENT_TASK

status: in_progress
started_at: 2026-08-30 Europe/Berlin
current_task: Проверить и исправить текущую логику итоговой сортировки/приоритета игр в витрине Steam KZ.
last_progress: Подтверждено происхождение двух стадий: builder → refiner намеренный, но две независимые формулы `priority_rank` являются лишним дублированием. Пользователь восстановил канонические правила: (1) среди автоматически ранжируемых допустимых кандидатов срочность скидки — верхний слой `сегодня → завтра → позже/неизвестно`; (2) подтверждённая фактическая проблема современной Windows/актуальная поддержка ниже Windows 10 понижает игру, но голая старая Steam-строка XP/7/8 без дополнительного подтверждения штрафа не даёт; (3) явное локальное действие пользователя `В конец очереди` имеет абсолютный приоритет над любым автоматическим `priority_rank`, включая скидку, заканчивающуюся сегодня/завтра. Последний пункт не должен менять production `priority_rank`: это сохранённый пользовательский UI override поверх канонического исходного порядка и он переживает новый суточный production до явного сброса пользовательского состояния. Все три правила записаны в `PROJECT_RULES.md`. `config/mailing_policy.json` пока ещё не отражает новый автоматический expiry layer и остаётся частью исправления.
next_step: Обновить machine policy под подтверждённые автоматические ranking rules и окончательно зафиксировать полный порядок факторов; затем вынести единственный canonical final priority sort после refinement. Отдельно проверить frontend/local-state применение `В конец очереди`, чтобы пользовательский override всегда накладывался после получения production order. Добавить regression-проверки для expiry urgency, legacy Steam-label neutrality, confirmed Windows demotion, policy ↔ implementation consistency и UI end-of-queue override precedence.
