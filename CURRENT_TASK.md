# CURRENT_TASK

status: in_progress
started_at: 2026-08-30 Europe/Berlin
current_task: Проверить и исправить текущую логику итоговой сортировки/приоритета игр в витрине Steam KZ.
last_progress: Подтверждено происхождение двух стадий: builder → refiner намеренный, но две независимые формулы `priority_rank` являются лишним дублированием. Пользователь дополнительно восстановил два канонических правила, уже записанных в `PROJECT_RULES.md`: (1) срочность окончания скидки становится самым верхним слоем финальной сортировки среди уже допустимых кандидатов — `заканчивается сегодня → заканчивается завтра → позже/неизвестно`; срочность не обходит taste/commercial eligibility; (2) подтверждённая фактическая ориентированность на Windows ниже 10 / необходимость fixes / проблемы современной Windows понижает игру, но одна только старая строка Steam XP/7/8 считается лишь сигналом для проверки и не даёт штрафа без подтверждения. Текущий `refine_visual_ranking.py` уже корректно нейтрализует голую legacy Steam label как `unknown`; новой логике нужен отдельный подтверждённый compatibility factor. `config/mailing_policy.json` пока ещё не отражает новый верхний expiry layer и остаётся частью исправления.
next_step: Обновить machine policy под подтверждённые правила и окончательно зафиксировать полный порядок факторов; затем вынести единственный canonical final priority sort после refinement и добавить regression-проверки для expiry urgency, legacy Steam-label neutrality, confirmed Windows demotion и policy ↔ implementation consistency.
