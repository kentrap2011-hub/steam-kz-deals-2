# CURRENT_TASK

status: in_progress
started_at: 2026-08-30 Europe/Berlin
current_task: Проверить и исправить текущую логику итоговой сортировки/приоритета игр в витрине Steam KZ.
last_progress: История GitHub подтвердила происхождение двух стадий. Первая priority-сортировка уже существовала в `build_daily_visual_payload.py`; к 2026-08-29 18:45 она учитывала Windows/risk/achievement/wishlist/history. Позже commit `d704d499` добавил отдельный `refine_visual_ranking.py`, который после сборки уточняет direct user evidence, personal risks, fit, commercial branch и duration, а commit `4919d515` намеренно подключил его как post-build refinement. Последующие commits `7895a973` и `896f19cb` специально защищали этот refinement от cross-snapshot применения, поэтому сам двухэтапный путь builder → refiner является намеренным. Однако оба этапа содержат независимые полные priority sort keys и refiner заново присваивает `priority_rank`; это дублирование не требуется для двухэтапного enrichment и создаёт расхождение policy ↔ implementation. Правильная граница: enrichment/refinement может оставаться отдельным этапом, но окончательная priority-сортировка должна иметь один канонический source of truth после завершения всех факторов. Отдельный `taste_rank` в refiner — диагностический ranking и не является дублем `priority_rank`.
next_step: Согласовать/восстановить канонический порядок факторов внутри одного priority_bucket, затем вынести финальный priority ordering в один источник истины, вызывать его после refinement и добавить regression-проверку соответствия policy.
