# CURRENT_TASK

status: in_progress
started_at: 2026-08-30 Europe/Berlin
current_task: Довести новую финальную сортировку до фактического `data/production/visual/current.json`, устранив блокирующий history/SteamDB gate без потери уже полученных данных.
last_progress: Новая ranking-архитектура реализована и regression-проверена, но `current.json` ещё старый. Точная причина блокировки восстановлена: `history_snapshot.json` имеет `primary_count=659`, из них 125 уже имеют persisted history status, а 534 отмечены `missing`; visual workflow требует `missing == 0`. Runtime state показывает 529 resolved из 534 и только 5 unresolved retry (`App_1282200`, `App_225320`, `App_399670`, `App_630060`, `App_901735`), у каждого ровно один retained transient failure `runtime_web_internal_error` из `one_time_recovery_migration`. Историческая причина all-or-nothing правила восстановлена и записана в `PROJECT_DECISIONS.md -> STEAMDB-001`: оно вводилось, чтобы batch/checkpoint никогда не превращался в суточную квоту и partial subset не объявлялся завершённым production stage. Это разумное completeness-требование. Позже реализация связала completion с persistence: `ingest_steamdb_runtime_submissions.py` выпускает final resolutions только при `unresolved_count == 0`, а checkpoint требует полный validated artifact. Поэтому корректное правило «529/534 = stage ещё не complete» превратилось в побочный эффект «529 подтверждённых фактов нельзя persist до закрытия 5». Это over-blocking не было исходной целью правила. Пять unresolved не подтверждены как постоянные SteamDB/data-проблемы: сохранённая классификация — tool/transport internal error, а не `unavailable_exact_history`; общего semantic паттерна для них в репозитории не найдено. 529 successes и 5 failures были мигрированы из старого runtime progress, поэтому сам текущий GitHub → external runtime → GitHub retry handoff ещё нужно фактически проверить, а не заменять ручным lookup из interactive chat.
next_step: Объяснить пользователю это различие и только после его понимания/решения менять архитектуру. Технически первым безопасным шагом остаётся bounded проверка/починка текущего runtime handoff на unresolved retry через owning production mechanism. Затем можно отдельно решить, сохраняем ли verified partial results при незавершённом stage, не ослабляя правило «partial batch != completed daily scope». После обновления history cache пересобрать history snapshot и canonical visual payload, затем проверить `FINAL-PRIORITY-RANKING-V1` в `current.json`. Windows compatibility evidence-source остаётся отдельным последующим участком.
