# CURRENT_TASK

status: in_progress
started_at: 2026-08-30 Europe/Berlin
current_task: Довести новую финальную сортировку до фактического `data/production/visual/current.json`, устранив блокирующий history/SteamDB gate без потери уже полученных данных.
last_progress: Новая ranking-архитектура реализована и regression-проверена, но `current.json` ещё старый. Точная причина блокировки восстановлена: `history_snapshot.json` имеет `primary_count=659`, из них 125 уже имеют persisted history status, а 534 отмечены `missing`; visual workflow требует `missing == 0`. При этом `data/cache/steamdb_runtime_work.json` показывает, что из текущего batch 534 уже `resolved_count=529`, а реально незакрыты только 5 retry: `App_1282200`, `App_225320`, `App_399670`, `App_630060`, `App_901735`, все после `runtime_web_internal_error`. Текущий SteamDB contract/all-or-nothing handoff не выпускает подготовленный `steamdb_web_resolutions.json` и не checkpoint-ит 529 результатов в `steamdb_history.json`, пока `unresolved_count != 0`; поэтому downstream видит не 5, а все 534 как missing. Это создаёт архитектурное over-blocking: пять временных ошибок удерживают 529 уже полученных фактов и весь visual rebuild. Это также конфликтует по смыслу с `PROJECT_RULES.md`: отсутствие второстепенной информации должно ухудшать полноту, но не приводить к потере потенциально хорошей покупки. Самый быстрый путь без изменения архитектуры — добиться успешного GitHub → external runtime → GitHub retry именно этих 5, после чего существующая chain сможет выпустить 534/534, validate, checkpoint, rebuild history snapshot и затем visual. Более устойчивое решение требует отдельного согласованного изменения SteamDB contract: разрешить checkpoint уже проверенных partial results и позволить visual build при небольшом числе unresolved history, маркируя их `unverified`, а retry продолжать отдельно; не просто ослаблять visual gate при старом cache, иначе потеряем полезные 529 history facts в текущем snapshot.
next_step: Сначала проверить/починить фактический runtime handoff для 5 unresolved retry и не выполнять их вручную в interactive chat. Затем решить, сохраняем ли all-or-nothing SteamDB contract или меняем его на partial verified checkpoint + nonblocking history for visual. После появления обновлённого `steamdb_history.json` пересобрать `history_snapshot`, запустить canonical visual build и проверить `FINAL-PRIORITY-RANKING-V1` в `current.json`. Windows compatibility evidence-source остаётся отдельным последующим незакрытым участком.
