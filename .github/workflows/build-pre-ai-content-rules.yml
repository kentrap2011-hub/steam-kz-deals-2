name: Build pre-AI content rules (manual)

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pre-ai-content-rules-manual
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build mechanical content rules for all candidates
        run: python scripts/build_pre_ai_content_rules.py
      - name: Commit pre-AI content rules
        run: |
          git config user.name "steam-kz-bot"
          git config user.email "steam-kz-bot@users.noreply.github.com"
          git add data/production/pre_ai/content_rules.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Refresh pre-AI content rules"
          for attempt in 1 2 3; do
            git fetch origin main:refs/remotes/origin/main
            if ! git rebase origin/main; then git rebase --abort || true; exit 1; fi
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
