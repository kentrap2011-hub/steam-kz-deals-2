# BACKLOG

Постоянный список задач проекта, которые **нужно сделать, но не сейчас**.

Назначение:
- сюда переносить подтверждённые проблемы и улучшения, которые сознательно откладываются ради более приоритетной работы;
- `CURRENT_TASK.md` использовать только для задачи, которая выполняется прямо сейчас;
- при выборе следующей работы просматривать этот файл и поднимать верхнюю подходящую задачу обратно в `CURRENT_TASK.md`;
- завершённые пункты удалять или переносить в историю/коммит, чтобы backlog оставался коротким.

## Обязательные свойства каждой задачи

У каждой задачи в backlog должны быть три оценки по относительной шкале `1..5`.

1. **Локальное исправление (`L`)** — сколько работы нужно непосредственно для изменения кода/конфига/документации.
   - `L1` — тривиальная правка: одна строка, флаг, очевидный config/doc fix;
   - `L2` — маленький локальный патч в одном месте;
   - `L3` — заметная правка/диагностика в одном маршруте;
   - `L4` — несколько компонентов или новая интеграция;
   - `L5` — исследование/архитектурная работа/большая реализация.

2. **Полный цикл (`T`)** — сколько времени/этапов нужно от начала локального исправления до фактически обновлённой и проверенной системы.
   - `T1` — результат готов сразу после локальной правки;
   - `T2` — нужен обычный commit + короткий CI/test;
   - `T3` — нужен rebuild/несколько штатных стадий;
   - `T4` — есть внешний/scheduled этап или заметное ожидание;
   - `T5` — несколько циклов, внешний runtime/retry или длительность существенно зависит от внешней системы.

3. **Важность (`I`)** — насколько сильно незакрытая задача влияет на полезность/надёжность системы.
   - `I1` — косметика/малозаметное улучшение;
   - `I2` — низкая важность, основной сценарий уже работает;
   - `I3` — средняя, заметно улучшает качество/полноту;
   - `I4` — высокая, влияет на важный production-сценарий;
   - `I5` — критическая/блокирующая. Критический incident, секрет или production outage не должен лежать в backlog — его нужно разбирать сразу по правилам проекта.

Оценки относительные: они нужны для сравнения задач между собой, а не как обещание точного времени. Их можно уточнять, когда появляется новая фактическая информация.

### Порядок backlog

По умолчанию задачи хранятся и выбираются в таком порядке:
1. меньше `L` — выше;
2. при одинаковом `L` меньше `T` — выше;
3. при одинаковых `L` и `T` больше `I` — выше.

То есть основной принцип пользователя: **сначала закрывать то, что можно исправить локально быстрее всего**, затем учитывать полный цикл, и только после этого важность как tie-breaker. Явно критические ситуации (`I5`, incident/security/outage) не ждут сортировки backlog.

## Отложенные задачи

### Получить и разобрать независимый taste-review текущей сортировки

**Статус:** waiting_external_review  
**L:** `L1`  
**T:** `T1`  
**I:** `I4`  
**Зафиксировано:** 2026-08-30  
**Handoff подготовлен:** `reviews/TASTE_SORT_REVIEW_HANDOFF.md`  
**Snapshot:** `data/production/visual/ranking_review.jsonl` на ref `ad4e8181a551669ff8031412d4dfe6f54bc03c35`  
**Причина оценки:** техническая передача уже подготовлена; следующий шаг — получить от отдельного taste-чата независимую диагностику и принести её обратно в основной проектный чат.

Что осталось:
- передать taste-чату готовый prompt и указать `reviews/TASTE_SORT_REVIEW_HANDOFF.md`;
- получить оценку спорных мест/системных паттернов;
- в основном чате отделить реальные taste/ranking/data проблемы от корректно объяснимых необычных позиций;
- только после этого решать, нужны ли изменения producer/ranking policy.

### UI: показывать причины текущей позиции по ranking-факторам

**Статус:** ready  
**L:** `L2`  
**T:** `T2`  
**I:** `I4`  
**Зафиксировано:** 2026-08-30  
**Причина оценки:** final producer уже записывает в каждую игру `priority_factors`, поэтому новый ranking engine не нужен. Основная работа — человекочитаемо показать существующие значения факторов на карточке и не дублировать вычисления в UI.

Что сделать:
- показать значения факторов, которыми фактически определяется `priority_rank`;
- использовать только готовые `priority_factors` из production payload, не пересчитывать sort key на клиенте;
- подписать факторы понятными названиями: срочность скидки, bucket, подтверждённый риск, wishlist, скидка, качество относительно истории цены, цена, достижения, длительность и tie-break;
- по возможности явно показывать, на каком первом факторе текущая игра выигрывает у следующей, чтобы было видно причину порядка соседних карточек;
- сохранить canonical factor order из `config/final_ranking_policy.json`.

### Risk: устранить карточки без понятного риска/статуса риска

**Статус:** ready_for_diagnosis  
**L:** `L3`  
**T:** `T3`  
**I:** `I4`  
**Зафиксировано:** 2026-08-30  
**Причина оценки:** producer уже задуман так, чтобы даже при отсутствии подтверждённого риска записывать информационный fallback `no_confirmed_personal_risk`. Если на части карточек риск не виден, сначала нужно определить, теряется ли поле в production, final producer или только в UI. После producer-исправления может понадобиться полный rebuild.

Что сделать:
- найти конкретные текущие карточки без отображаемого риска;
- проверить `risk`, `risk_level`, `risk_source`, `risk_max_severity` и `personal_risks` в production payload;
- определить точку потери: enrichment → final producer → payload → UI;
- гарантировать, что каждая карточка показывает либо подтверждённый риск, либо явное «подтверждённых персональных рисков сейчас нет»;
- добавить regression check, чтобы поле снова не исчезало незаметно.

### SteamDB history: закрыть оставшиеся retry и проверить штатный handoff

**Статус:** deferred  
**L:** `L3`  
**T:** `T5`  
**I:** `I2`  
**Зафиксировано:** 2026-08-30  
**Причина оценки:** локально требуется диагностика/починка штатного handoff, но полный результат зависит от внешнего scheduled runtime, retry, ingestion, validation, checkpoint и downstream rebuild. Основной history-сценарий уже работает для 529 из 534 ключей, поэтому важность сейчас низкая.

Что осталось:
- GitHub-derived runtime work содержит 534 SteamDB history ключа: 529 resolved, 5 unresolved;
- оставшиеся ключи: `App_1282200`, `App_225320`, `App_399670`, `App_630060`, `App_901735`;
- последний известный failure для них: `runtime_web_internal_error`;
- нужно не обрабатывать их вручную в interactive chat, а довести до рабочего состояния штатный GitHub → scheduled runtime handoff;
- после получения evidence GitHub должен сам выполнить ingestion/validation/checkpoint, обновить canonical history cache и downstream artifacts;
- задача считается закрытой только после `unresolved_count == 0` и успешного end-to-end downstream rebuild.

При возврате к задаче сначала сверяться с `config/execution_ownership_contract.json`, `config/steamdb_lookup_contract.json` и актуальным `data/cache/steamdb_runtime_work.json`.

### YouTube: добавить на карточку выбранный русскоязычный обзор игры

**Статус:** deferred  
**L:** `L4`  
**T:** `T4`  
**I:** `I3`  
**Зафиксировано:** 2026-08-30  
**Причина оценки:** простую ссылку на результаты поиска YouTube добавить легко, но это не выполняет запрос пользователя на конкретный русскоязычный обзор. Для полноценного варианта нужен источник/выбор видео, проверка релевантности и языка, поле в production payload и UI-переход.

Что сделать:
- определить автоматический способ находить релевантный русскоязычный обзор конкретной игры;
- не подменять задачу простой ссылкой на поиск, если можно получить выбранный проверенный ролик;
- хранить выбранный URL/идентификатор и минимальные сведения о ролике в production payload;
- добавить на каждую карточку компактный блок/кнопку перехода на YouTube;
- определить fallback для игр, где подходящий русскоязычный обзор не найден;
- не выполнять web-поиск из read-only UI во время просмотра карточки.

### Windows compatibility: добавить надёжный automatic evidence-source

**Статус:** deferred  
**L:** `L4`  
**T:** `T4`  
**I:** `I3`  
**Зафиксировано:** 2026-08-30  
**Причина оценки:** final ranking уже умеет учитывать подтверждённый `modern_windows_friction`, но отдельный надёжный автоматический источник этих фактов пока не подтверждён. Потребуется выбрать/проверить источник, встроить его в существующий ownership/data route и добавить validation/tests; после локальной реализации понадобится штатный production rebuild.

Что осталось:
- определить надёжный источник фактических проблем совместимости игр с современными Windows;
- не использовать голую legacy Steam system-requirements строку как достаточное подтверждение риска;
- встроить evidence в существующий data/ownership route без второго независимого sort-layer;
- проверить, что подтверждённый `modern_windows_friction` действительно доходит до final producer и влияет на ranking только в уже предусмотренном месте;
- добавить negative-path/regression validation и выполнить production rebuild.

При возврате сначала смотреть раздел «Финальная сортировка / priority rank витрины» в `PROJECT_ROUTES.md`, `config/final_ranking_policy.json` и `config/execution_ownership_contract.json`.
